# Firmware

The builder reports that the prototype's hardware and software are about 90% complete. The existing firmware has **not been uploaded to this repository yet**.

This folder is reserved for that source and its setup instructions. There is no generated replacement firmware, executable release, or verified flashing procedure here.

When the existing code is added, document its framework/version, required libraries, display driver and address, serial settings, music-card layout, and button actions. The current physical assignments are in the [Rev 1 pinout](../docs/pinout.md).
